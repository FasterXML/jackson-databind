/**
Package that contains handlers specific to datatypes introduced in Java 8.
Previously these were added via additional external modules, but with Jackson 3
most (if not all) types are included directly in databind.

@since 3.0
*/

package com.fasterxml.jackson.databind.ext.jdk8;
