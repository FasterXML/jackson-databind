/**
 * Package that contains standard value and key deserializer implementations
 * Jackson uses for its own public types.
 * Note that these implementations are not considered as part of either public
 * API (like {@code ObjectMapper} or "developer-public" API (such as
 * {@code com.fasterxml.jackson.databind.deser.std}): implementations and
 * interfaces may change.
 * It is not advisable for non-core code to rely on these types.
 */
package com.fasterxml.jackson.databind.deser.jackson;
